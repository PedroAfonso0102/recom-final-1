export const colors = {
    brand: "var(--wire-brand)",
    text: "var(--wire-text)",
    muted: "var(--wire-muted)",
    border: "var(--wire-border)",
    surface: "var(--wire-surface)",
    success: "var(--wire-success)",
    danger: "var(--wire-danger)",
};
if (typeof window !== 'undefined') window.RECOM_DS_COLORS = colors;
