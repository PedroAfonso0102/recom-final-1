export const typography = {
    family: "Inter, sans-serif",
    sizes: {
        sm: "0.85rem",
        md: "1rem",
        lg: "1.25rem",
        xl: "1.5rem",
        xxl: "2rem",
    }
};
if (typeof window !== 'undefined') window.RECOM_DS_TYPOGRAPHY = typography;
